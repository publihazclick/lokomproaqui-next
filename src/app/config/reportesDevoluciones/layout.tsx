export const metadata = {
  title: 'Reportes de Devoluciones | LokomproAqui',
};

export default function ReportesDevolucionesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
