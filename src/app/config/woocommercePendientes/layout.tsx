export const metadata = {
  title: 'Pedidos WooCommerce por Revisar | LokomproAqui',
};

export default function WoocommercePendingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
